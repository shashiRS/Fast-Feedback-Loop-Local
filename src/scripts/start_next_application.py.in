#! /usr/bin/env python3
"""
Script for starting an application of the Next framework.

Do not change this file, it is generated during a CMake run.

The script is starting the @STARTUPAPPLICATION@ out of the conan cache dir. So the application and its libraries do not
have to be installed.
"""

import os
import subprocess
import sys

startup_application = "@STARTUPAPPLICATION@"
next_conan_cache_dir = "@NEXTCONANCACHEDIR@/bin"

if not sys.platform.startswith("linux"):
    sys.exit("Not supported platform, only Windows and Linux are supported")

try:
    proc = subprocess.Popen("gnome-terminal -- " + os.path.join(next_conan_cache_dir, startup_application), shell=True)
except Exception as e:
    print("Failed to start:", e)
